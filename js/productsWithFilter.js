function renderProductsFilter(arrayProducts){
    // arrayProducts.forEach(element => {
    //     console.log(element);
    //  });
    // console.log(arrayProducts);
    let dataResponsive = JSON.parse(arrayProducts);
    console.log(arrayProducts);
 };
